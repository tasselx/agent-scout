# agent-scout 环境变量（可选）。多数情况下无需设置——
# 程序会从本地 Devin/Windsurf 安装自动提取 key。
#
# 显式设置时（优先级最高）：
WINDSURF_API_KEY=devin-session-token$replace-me

# 兼容旧版变量名（优先级次之）：
# WINDSURFAPI_CODEIUM_API_KEY=devin-session-token$replace-me

# 平台路径覆盖（主要用于测试 WSL/Windows 路径发现）：
# APPDATA=C:\Users\<user>\AppData\Roaming
# XDG_CONFIG_HOME=/home/<user>/.config